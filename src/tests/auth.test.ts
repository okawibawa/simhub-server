import { describe, it, expect, vi, test } from "vitest";

global.fetch = vi.fn();

describe("authentication", () => {
  test.todo("make a post request to sign up");
  test.todo("make a post request to sign in");
  test.todo("make a post request to sign out");
});
